package sample;

public class XOR extends Compuerta { // Clase para la compuerta Xor que hereda de la clase Compuerta
    public XOR(boolean ValorV, Compuerta n, ListaPrevs p) {
        super(ValorV, n, p);
    }

}
